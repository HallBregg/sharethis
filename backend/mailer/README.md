## Jobs
### flake8
This job should be run in `dev` stage of a Dockerfile.

Command:
```shell
flake8 src
```

### mypy
This job should be run in `dev` stage of a Dockerfile.

Command:
```shell
mypy src
```
x
